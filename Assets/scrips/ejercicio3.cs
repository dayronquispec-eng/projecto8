using System;
using UnityEngine;

public class ejercicio3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int i = 1; i <= 5; i++)
        {
            Debug.Log("Enemigo generado"+i);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
