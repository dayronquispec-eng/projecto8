using UnityEngine;

public class ejercicio4 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int i = 0; i <= 5; i++)
        {
            Debug.Log("Iniciando ronda");
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
