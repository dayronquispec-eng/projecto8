using UnityEngine;

public class ejercicio5 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        for (int i = 1; i <= 5; i++)
        {
            Debug.Log("Iniciando ronda" + i);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
